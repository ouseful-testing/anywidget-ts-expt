{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/*.ts --minify --format=esm --bundle --outdir=src/anywidget_ts_expt/static  --loader:.html=text",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@wokwi/elements": "^1.5.1",
		"avr8js": "^0.20.1"
	},
	"devDependencies": {
		"@anywidget/types": "^0.1.9",
		"esbuild": "^0.23.0",
		"typescript": "^5.5.3"
	}
}
